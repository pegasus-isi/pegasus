tr wc {
    site {{sitename}} {
        pfn "/usr/bin/wc"
        arch "{{arch}}"
        os "{{os}}"
        type "INSTALLED"
    }
}

tr split {
    site {{sitename}} {
        pfn "/usr/bin/split"
        arch "{{arch}}"
        os "{{os}}"
        type "INSTALLED"
    }
}

