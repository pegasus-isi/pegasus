tr ls {
    site {{sitename}} {
        pfn "/bin/ls"
        arch "{{arch}}"
        os "{{os}}"
        type "INSTALLED"
    }
}

tr cat {
    site {{sitename}} {
        pfn "/bin/cat"
        arch "{{arch}}"
        os "{{os}}"
        type "INSTALLED"
    }
}

