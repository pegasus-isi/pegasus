#!/bin/sh
#
# Show the version number. May be used to show even more (e.g. build)
# $Id: pegasus-version,v 1.3 2005/07/13 16:45:22 griphyn Exp $
#
. "`dirname $0`/common.sh"

# run java program
${JAVA_HOME}/bin/java "-pegasus.home=${PEGASUS_HOME}" $addon org.griphyn.vdl.toolkit.VersionNumber "$@"
