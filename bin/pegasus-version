#!/bin/sh
#
# Show the version number. May be used to show even more (e.g. build)
# $Id$
#
. "`dirname $0`/common.sh"

# run java program
${JAVA} "-Dpegasus.home=${PEGASUS_HOME}" $addon org.griphyn.vdl.toolkit.VersionNumber "$@"
