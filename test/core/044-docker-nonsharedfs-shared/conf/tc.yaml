pegasus: "5.0"
transformations:
  - namespace: diamond
    name: analyze
    version: 4.0
    sites:
      - name: local
        arch: x86_64
        os.type: linux
        container: centos-base
        pfn: ${PEGASUS_LOCAL_BIN_DIR}/pegasus-keg
        type: stageable
        profile:
          condor:
            foo: bar
  - namespace: diamond
    name: findrange
    version: 4.0
    sites:
      - name: local
        arch: x86_64
        os.type: linux
        container: centos-base
        pfn: ${PEGASUS_LOCAL_BIN_DIR}/pegasus-keg
        type: stageable
        profile:
          condor:
            foo: bar
  - namespace: diamond
    name: preprocess
    version: 4.0
    sites:
      - name: local
        arch: x86_64
        os.type: linux
        container: centos-base
        pfn: ${PEGASUS_LOCAL_BIN_DIR}/pegasus-keg
        type: stageable
        profile:
          condor:
            foo: bar

containers:
  - image: docker:///centos.type:7
    profile:
      env:
        APP_HOME: /tmp/myscratch
        HELLo: WORLD
        JAVA_HOME: /bin/java.1.6
    name: centos-base
    image_sites: local
    type: docker
    mount:
      - /lizard:/lizard
